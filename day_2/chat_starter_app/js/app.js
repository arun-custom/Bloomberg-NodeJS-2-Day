//Connect to the socket server with Socket.io

//Set up a socket listener for the event "chat". Call the function "getMessage" inside of the callback passing in the argument from the callback function.

function sendMessage(message) {
	//Emit the variable "message" back to the server with an event called "chat"
}